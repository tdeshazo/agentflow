apiVersion: agentflow.dev/v1alpha2
kind: AgentWorkflow
metadata:
  name: invalid-v1alpha2-unknown-field
spec:
  workspace:
    allowWrites: [src/**]
    unknown: true
  agents:
    coder:
      runner: codex
      model: gpt-5.6-terra
  validation:
    tests:
      run: make test
  phases:
    - id: implement
      actor: coder
      prompt: Implement the feature.
      validation: tests
  completion:
    validation: tests
